import styled from "@emotion/styled";

export const Wrapper = styled.div`
  height: 200px;
  background-color: skyblue;
`;

export default function LayoutNavigationPage() {
  return <Wrapper>내비게이션 영역 (NavigationPage)</Wrapper>;
}
