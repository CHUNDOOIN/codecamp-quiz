import styled from "@emotion/styled";

export const Wrapper = styled.div`
  height: 200px;
  background-color: pink;
`;

export default function LayoutBannerPage() {
  return <Wrapper>배너 영역 (BannerPage)</Wrapper>;
}
