import styled from "@emotion/styled";

export const Wrapper = styled.div`
  height: 200px;
  background-color: greenyellow;
`;

export default function LayoutHeaderPage() {
  return <Wrapper>헤더 영역 (HeaderPage)</Wrapper>;
}
